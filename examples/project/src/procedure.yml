procedure:
  name: "src"
  build:
    compiler: "clang"
    files:
      - "test.c"
  link:
    files:
      - "test.o"
    into: "bin"
    linker: "clang"
    with:
      - name: "EpicLibrary"
        kind: "exported"

      - name: "m"
        kind: "compiler"

      - name: "cairo"
        kind: "pkg-config"
