procedure:
  name: "src"
  build:
    files:
      - "test.c"
  link:
    files:
      - "test.o"
    into: "test"
    with:
      - name: "EpicLibrary"
        kind: "exported"

      - name: "m"
        kind: "compiler"

      - name: "cairo"
        kind: "pkg-config"
